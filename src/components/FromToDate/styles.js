const styles = {
    container: {
        display: "flex",
        alignContent: "flex-start",
        alignItems: "baseline",
    },

    label: {
        padding: "0 30px"
    }
}

export default styles;