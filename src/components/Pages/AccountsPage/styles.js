const styles = {
    container: {
        display: "flex",
        alignContent: "center",
        justifyContent: "center",
        alignItems: "baseline",
        paddingTop: 50
    }
}

export default styles;